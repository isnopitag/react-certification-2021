import React, { useReducer } from 'react';
import { render } from '@testing-library/react';
import {Hamburger} from './Hamburger'

describe('Testing on hamburger', () => {
        it('must trigger the function', () => {
            const hamburger = render(<Hamburger/>);
            
        })
        
})
